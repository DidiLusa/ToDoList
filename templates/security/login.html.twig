{% extends 'base.html.twig' %}

{% block title %}Log in!{% endblock %}

{% block body %}
    <h1>Bienvenue sur le site My To do list</h1>
   <p>Lorem ipsum dolor sit amet, consectetur adipisicing elit. Nobis voluptatum
    eos voluptatibus libero consequatur amet quo ipsa molestiae commodi? Maxime 
    rem magnam recusandae consectetur consequuntur eius error maiores dolore odio. 
    Debitis id est facilis ratione, magni ipsam reiciendis tempore sed corporis 
    consectetur architecto non veniam soluta, consequatur, laudantium quae. 
    Molestiae nulla repellat aut accusantium quam officiis optio repudiandae facilis recusandae.</p>
   
   <h3 class="h3 mb-3 font-weight-normal">Connexion</h3>
<form method="post">
    {% if error %}
        <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
    {% endif %}

    {% if app.user %}
        <div class="mb-3">
            You are logged in as {{ app.user.username }}, <a href="{{ path('app_logout') }}">Logout</a>
        </div>
    {% endif %}

    <label for="inputEmail">Votre emaill</label>
    <input type="email" value="{{ last_username }}" name="email" id="inputEmail" class="form-control" required autofocus>
    <label for="inputPassword">Votre mot de passe</label>
    <input type="password" name="password" id="inputPassword" class="form-control" required>

    <input type="hidden" name="_csrf_token"
           value="{{ csrf_token('authenticate') }}"
    >

    {#
        Uncomment this section and add a remember_me option below your firewall to activate remember me functionality.
        See https://symfony.com/doc/current/security/remember_me.html

        <div class="checkbox mb-3">
            <label>
                <input type="checkbox" name="_remember_me"> Remember me
            </label>
        </div>
    #}

    <button class="btn btn-lg btn-primary" type="submit">
        Connexion
    </button>
</form>
    <p>Pas encore de compte ? </p>
    <a href="{{ path('app_register')}}">Inscrivez-vous</a>
{% endblock %}
